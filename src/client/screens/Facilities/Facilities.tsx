import React from 'react';
import { ScreenContainer, TextTitle } from '~/client/components';

export const Facilities: React.FC = () => {
  return (
    <ScreenContainer>
      <TextTitle>Facilities</TextTitle>
    </ScreenContainer>
  );
};
