# Migration `20190809214904`

This migration has been generated by Miguel Dorta at 8/9/2019, 9:49:04 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "development"."User"("id" text NOT NULL  ,"createdAt" timestamp(3) NOT NULL  ,"username" text NOT NULL DEFAULT '' ,"password" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "development"."Vault"("id" text NOT NULL  ,"createdAtTick" integer NOT NULL DEFAULT 0 ,"x" integer NOT NULL DEFAULT 0 ,"y" integer NOT NULL DEFAULT 0 ,"userId" text   ,"resourceId" text NOT NULL DEFAULT '' ,"facilityId" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "User.username._UNIQUE" ON "development"."User"("username")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190809214904
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,26 @@
+datasource db {
+  provider = "postgresql"
+  url = env("POSTGRES_URL")
+}
+
+generator photonjs {
+  provider = "photonjs"
+  output = "../src/server/photon"
+}
+
+model User {
+  id String @id @default(uuid())
+  createdAt DateTime @default(now())
+  username String @unique
+  password String
+}
+
+model Vault {
+  id String @id @default(uuid())
+  createdAtTick Int
+  x Int
+  y Int
+  userId String?
+  resourceId String
+  facilityId String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190809214904)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190809214904';

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
