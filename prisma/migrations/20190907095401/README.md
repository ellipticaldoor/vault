# Migration `20190907095401`

This migration has been generated by Miguel Dorta at 9/7/2019, 9:54:01 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "development"."GameState"("id" SERIAL,"ticks" integer NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"))
;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190907093427..20190907095401
--- datamodel.dml
+++ datamodel.dml
@@ -7,8 +7,13 @@
   provider = "photonjs"
   output = "../src/photon"
 }
+model GameState {
+  id Int @id
+  ticks Int
+}
+
 model User {
   id String @id @default(uuid())
   createdAt DateTime @default(now())
   username String @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20190907095401)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190907095401';

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
