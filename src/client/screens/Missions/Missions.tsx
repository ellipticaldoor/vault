import React from 'react';
import { ScreenContainer, TextTitle } from '~/client/components';

export const Missions: React.FC = () => {
  return (
    <ScreenContainer>
      <TextTitle>Missions</TextTitle>
    </ScreenContainer>
  );
};
